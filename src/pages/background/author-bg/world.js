import React from 'react'
import { Link } from "gatsby";
import Layout from "../../../components/Layout";
import World from "../../../components/essay/World";
import { Button, Row, Col } from "react-bootstrap";
const world = () => {
    return (
      <Layout>
        <World/>
      </Layout>
    );
}

export default world
